import { BlogPostTagSizePipe } from './blog-post-tag-size.pipe';

describe('BlogPostTagSizePipe', () => {
  it('create an instance', () => {
    const pipe = new BlogPostTagSizePipe();
    expect(pipe).toBeTruthy();
  });
});
